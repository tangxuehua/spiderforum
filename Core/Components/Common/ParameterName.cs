namespace System.Web.Core
{
    public enum ParameterName
    {
        UserId,
        MemberId,
        RoleId,
        PageIndex,
        PictureWidth,
        PictureHeight,
        FileUrl,
        Tags,
        FileName,
        PathFolder,
        ExceptionLogId
    }
}